@model IEnumerable<TutorMeNow.Models.MathWordProblem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MathQuestion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MathAnswer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MathQuestion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MathAnswer)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MathId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MathId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MathId })
        </td>
    </tr>
}

</table>
